# Server
NODE_ENV=development
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info

# MongoDB
MONGODB_URI=mongodb://localhost:27017/user_mgmt

# Auth
# Use a long random secret (32+ bytes). In prod, store this in a secrets manager.
JWT_SECRET=change-me-super-secret
JWT_ISSUER=user-management-service
JWT_AUDIENCE=learning-platform
JWT_EXPIRES_IN_SECONDS=900

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000


